== waypoint_manager

Simple rails application for managing waypoints. It can:

* simple CRUD for waypoints and waypoint regions

* import files from GPS device

* export to GPX file usable in GPS device