!!!
%html

  %head
    %title
      = yield(:title) || "Untitled"
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag "application"
    = javascript_include_tag :application
    = csrf_meta_tag
    = yield(:head)

  %body
    #container
      #menu
        = link_to "Areas", areas_path
        |
        = link_to "Waypoints", waypoints_path(page: 1)
        |
        = link_to "Importer", new_importer_path
        |
        Export to GPX:
        = link_to "all", exporter_path
        ,
        = link_to "private", exporter_path(private: true)
        ,
        = link_to "public", exporter_path(private: false)

      %hr

      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"

      - if show_title?
        %h1= yield(:title)

      = yield
