= simple_form_for resource, html: {class: "form-horizontal"} do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :url
    = f.input :area_id, as: :select, collection: Area.order(:name).all, include_blank: false

  .form-actions
    = f.button :submit
