- default_full = false
- title = "#{t('new')} - #{t('area.title')}"
- content_for :title, title
%h1= title

= render 'form'

.btn-group
  - if can?(:read, resource.class)
    = link_to collection_path, class: 'btn' do
      %i.icon-search
      = t(:back)
