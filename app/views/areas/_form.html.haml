= simple_form_for resource, html: {class: "form-horizontal"} do |f|
  = f.input :name
  = f.input :area_type, collection: Area::TYPES.keys, include_blank: false
  = f.submit 'Save', class: 'btn btn-success'
